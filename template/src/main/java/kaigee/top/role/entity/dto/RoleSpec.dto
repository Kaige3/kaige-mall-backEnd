
input RoleSpecInput {
    #allScalars(RoleSpec)
    id?
}

specification RoleSpecSpec {
    #allScalars
    like/i(getName)
    like/i(getId)
    ge(getMaxCreatedTime)
    le(getMaxCreatedTime)
    ge(getMaxEditedTime)
    le(getMaxEditedTime)
    ge(getMinEditedTime)
    le(getMinEditedTime)
    ge(getMinCreatedTime)
    le(getMinCreatedTime)
}