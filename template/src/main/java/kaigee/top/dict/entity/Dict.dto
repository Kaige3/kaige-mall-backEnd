
input DictInput {
    #allScalars(Dict)
    id?
}

specification DictSpec {
    #allScalars
    like/i(keyName)
    like/i(dictEnName)
    like/i(dictName)
    like/i(keyEnName)
    like/i(id)
    ge(editedTime)
    le(editedTime)
    ge(createdTime)
    le(createdTime)
}