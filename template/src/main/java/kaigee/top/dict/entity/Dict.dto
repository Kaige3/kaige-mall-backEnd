
input DictInput {
    #allScalars(Dict)
    id?
}

specification DictSpec {
    #allScalars
    like/i(dictEnName)
    like/i(dictName)
    like/i(keyEnName)
    like/i(keyName)
    like/i(id)
    ge(createdTime)
    le(createdTime)
    ge(editedTime)
    le(editedTime)
}