
input UserInput {
    #allScalars(User)
    id?
}

specification UserSpec {
    #allScalars
    like/i(password)
    like/i(id)
    like/i(status)
    like/i(nickname)
    like/i(phone)
    ge(editedTime)
    le(editedTime)
    ge(createdTime)
    le(createdTime)
}