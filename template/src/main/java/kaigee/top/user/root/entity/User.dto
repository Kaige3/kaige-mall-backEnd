
input UserInput {
    #allScalars(User)
    id?
}

specification UserSpec {
    #allScalars
    like/i(password)
    like/i(id)
    like/i(status)
    like/i(phone)
    like/i(nickname)
    ge(createdTime)
    le(createdTime)
    ge(editedTime)
    le(editedTime)
}