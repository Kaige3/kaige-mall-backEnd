
input UserInput {
    #allScalars(User)
    id?
}

specification UserSpec {
    #allScalars
    like/i(password)
    like/i(nickname)
    like/i(phone)
    like/i(gender)
    like/i(id)
    ge(createdTime)
    le(createdTime)
    ge(editedTime)
    le(editedTime)
}